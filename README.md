# touchline-website

## Tailwind CSS (theme)

Tailwind is integrated in `touchline-theme`.

```bash
cd touchline-theme
npm install
npm run build:tailwind
```

Use `npm run watch:tailwind` while developing to automatically rebuild `assets/css/tailwind.css`.
